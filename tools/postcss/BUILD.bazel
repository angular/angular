load("//tools:defaults.bzl", "ts_library")

package(default_visibility = ["//visibility:public"])

ts_library(
    name = "postcss",
    srcs = [
        "compare-nodes.ts",
    ],
    deps = [
        "@npm//postcss",
    ],
)
