import { DocMetadata } from '../model';
import { SiteMapService } from './sitemap.service';

xdescribe('SiteMapService', () => {
  it('can create service', () => {
    expect(true).toBe(true);
  });
});
