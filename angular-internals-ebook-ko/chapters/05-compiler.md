# 5장: 컴파일러의 마법

> *"내 템플릿에 무슨 일이 일어날까?"*

## 컴파일 파이프라인

```
템플릿 문자열 → 파싱 → 변환 → 생성 → JavaScript
```

## AOT vs JIT

- **AOT** (Ahead-of-Time): 빌드 중 컴파일 → 작은 번들, 빠른 시작
- **JIT** (Just-in-Time): 브라우저에서 컴파일 → 느림, 번들에 컴파일러 포함

**프로덕션에서는 항상 AOT 사용!**

**다음**: [6장: Zone.js 심층 분석](06-zone-js.md)
