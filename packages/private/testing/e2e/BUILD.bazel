package(default_visibility = ["//packages:__subpackages__"])

load("//tools:defaults.bzl", "ts_library")

ts_library(
    name = "e2e",
    testonly = True,
    srcs = ["index.ts"],
    deps = [
        "@ngdeps//@types/selenium-webdriver",
        "@ngdeps//selenium-webdriver",
    ],
)

filegroup(
    name = "on_prepare",
    testonly = True,
    srcs = [
        "protractor.on_prepare.js",
    ],
)
