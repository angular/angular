load("//tools:defaults.bzl", "ng_module")

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "experimental",
    srcs = glob(
        [
            "*.ts",
            "src/**/*.ts",
        ],
        exclude = [
            # Exclude files that don't build
            "src/custom-schemas/**",
            "src/kirjs-idea1/**",
        ],
    ),
    deps = [
        "//packages/core",
        "//packages/forms",
        "@npm//@standard-schema/spec",
    ],
)
