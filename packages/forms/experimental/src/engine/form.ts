import {WritableSignal} from '@angular/core';
import {FormFieldImpl, formNode} from './field';
import {FormField, FormNode} from './types';

/**
 *
 * rule(f.abc // FormNode, [
 *   disable(...)
 * ]);
 *
 */
