package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ng_module")

ng_module(
    name = "testing",
    # TODO(i): disabled so that ng_package can depend on it
    # testonly = 1,
    srcs = glob(["**/*.ts"]),
    module_name = "@angular/common/testing",
    deps = [
        "//packages/common",
        "//packages/core",
        "@rxjs",
    ],
)
