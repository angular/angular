package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ng_module")

ng_module(
    name = "config",
    srcs = glob([
        "*.ts",
        "src/**/*.ts",
    ]),
    deps = ["//packages/core"],
)
