load("//tools:defaults.bzl", "ts_project")

package(default_visibility = [
    "//packages/core:__subpackages__",
])

ts_project(
    name = "triggers",
    srcs = glob(
        [
            "**/*.ts",
        ],
    ),
)
