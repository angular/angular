load("//adev/shared-docs:defaults.bzl", "copy_to_bin")

exports_files(
    glob(["*"]),
)

copy_to_bin(
    name = "images",
    srcs = glob(
        include = ["**/*"],
        exclude = ["BUILD.bazel"],
    ),
    visibility = [
        "//visibility:public",
    ],
)
