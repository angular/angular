machine:
  node:
    version: 5.4.1

dependencies:
  pre:
    - npm install -g npm
  override:
    - npm install:
        environment:
          # Token for typings to increase github rate limit
          # See https://github.com/typings/typings/blob/master/docs/faq.md#configuration
          # This is not hidden using https://circleci.com/docs/fork-pr-builds#details
          # because those are not visible for pull requests, and those should also be reliable.
          # This SSO token belongs to github account angular-github-ratelimit-token which has no access
          # (password is in Valentine)
          typings_githubToken: ef474500309daea53d5991b3079159a29520a40b

test:
  override:
    - npm run build
