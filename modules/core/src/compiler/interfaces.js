import {TemplateElement, StyleElement} from 'facade/dom';
import {DirectiveMetadata} from './directive_metadata';

export class OnChange {
  onChange(changes) {
    throw "OnChange.onChange is not implemented";
  }
}
