{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "/content/docs-prerendered/:path*.html"
      }
    ]
  }
}
